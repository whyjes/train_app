name: train_app
description: Train query system.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  csv: ^6.0.0
  file_picker: ^8.0.0
  path_provider: ^2.1.0
flutter:
  uses-material-design: true
  assets:
    - assets/data.csv
